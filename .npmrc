# The shared packages live on GitHub Packages, not on npmjs.com. Only the scope
# mapping is committed; the read token stays out of the repository — it is in
# ~/.npmrc on a workstation and arrives as a BuildKit secret in CI, so it never
# becomes a layer, a build argument or a line in git history.
@camircode:registry=https://npm.pkg.github.com
